const rootURL = "https://ui-assessment-backend.onrender.com";

const addURL = rootURL + "/operations/add";
const updateURL = rootURL + "/operations/update";
const countURL = rootURL + "/operations/count";

export { addURL, updateURL, countURL };
